# Basic Types:
# 1) Integers
# 2) Floats
# 3) Booleans
# 4) Atoms
# 5) Strings
# 6) Lists
# 7) Tuples

IO.inspect "1 + 2 = #{1+2}"
IO.inspect "5 * 5 = #{5*5}"
IO.inspect "10 / 2 = #{10/2}"

# notice that / returns a float 5.0 instead of integer 5.
# the / returns a float.
# Use div and rem to return integer division and division remainder

div(10,2)
IO.inspect "div(10,2) returns #{div(10,2)}"
IO.inspect "div 10,2 returns #{div 10,2 }"
IO.inspect "rem 10, 3 returns #{rem 10,3}"

# allows parenthesis to be ommited in named functions for clearer syntax when
# writing declarations and control-flow constructs.

# supports binary, octal, and hexadecimal

# switch over to interactive elixir
# evaluate 0b1010, 0o777, and 0x1F
# evaluate 10e-10
# evaluate round(3.58) => 4
# evaluate trunc(3.58) => 3





















e
